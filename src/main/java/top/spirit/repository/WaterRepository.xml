<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.spirit.repository.WaterRepository">
    <select id="findAll" resultType="top.spirit.entity.Water">
        select * from monitor_wqi
    </select>
    <select id="findNewData" resultType="top.spirit.entity.Water">
        SELECT * FROM monitor_wqi order by water_id DESC limit 1;
        </select>
</mapper>
